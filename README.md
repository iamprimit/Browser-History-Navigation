# Browser-History-Navigation
Stack-based browser history simulation. Supports visit(), back(n), forward(n), go(n), and showHistory(). Demonstrates how real browsers manage navigation using back and forward stacks.
